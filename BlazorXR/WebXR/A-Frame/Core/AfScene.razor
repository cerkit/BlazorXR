<a-scene embedded="@(Embedded ? "true" : "")">
    @ChildContent
</a-scene>

@code {
    [Parameter]
    public bool Embedded { get; set; } = false;

    [CascadingParameter]
    public RenderFragment? ChildContent { get; set; }
}

