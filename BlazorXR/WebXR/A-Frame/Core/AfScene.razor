﻿<a-scene embedded="@(Embedded ? "embedded" : "")">
    @ChildContent
</a-scene>

@code {
    [Parameter]
    public bool Embedded { get; set; } = false;

    [Parameter]
    public RenderFragment? ChildContent { get; set; }
}

